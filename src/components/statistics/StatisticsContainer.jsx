export default function StatisticsContainer({ children }) {

    return (

        <div
            className="
                stats w-full h-20 mb-0 grid grid-cols-5 gap-3">

            {children}

        </div>

    );

}